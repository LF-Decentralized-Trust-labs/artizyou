const initialState = {
  data: {},
};

const creationTypes = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default creationTypes;